# easy-ha-proxy

[English](README.md) | [Русский](README.ru.md)

Начните с [краткого руководства](QUICKSTART.ru.md).

easy-ha-proxy — production-ориентированный одноузловой стек HAProxy, Let's
Encrypt, GeoIP ACL, Authelia и веб-интерфейса управления. Один помощник
устанавливает и обслуживает сервисы хоста, контейнеры, сертификаты, правила
доступа и зашифрованные резервные копии для аварийного восстановления.

## Возможности

- HAProxy для HTTP/HTTPS- и TCP-прокси, health checks, rate limits, блокировок,
  maintenance-страниц и необязательной GeoIP-фильтрации. Одна локальная база
  DB-IP Country Lite используется и для стран в UI, и для IPv4/IPv6 ACL
  HAProxy; флаги встроены в приложение, поэтому во время работы внешние API не
  вызываются.
- Проброс UDP через ядерный NAT (iptables DNAT) для сервисов, которые HAProxy
  проксировать не умеет (например WireGuard), с учётом того же набора банов и
  необязательным zero-trust-контролем (только авторизованные IP).
- Автоматический выпуск и продление сертификатов Let's Encrypt, внутренний CA
  со скачиваемым публичным корневым сертификатом и проверка цепочек внешних CA.
- Авторизация Authelia с Redis и файловой базой пользователей, а также
  необязательные клиентские сертификаты (mTLS) для отдельных сайтов —
  самостоятельный слой перед ней.
- Веб-интерфейс для сайтов, TCP-прокси, проброса UDP, сертификатов,
  пользователей и политик Authelia, runtime-блокировок, состояния системы и
  безопасного изменения конфигурации HAProxy. Интерфейс поддерживает русский и
  английский языки и запоминает выбор пользователя.
- Локальный и SSH-установщик с диагностикой, check mode, точечными обновлениями,
  repair, миграцией домена и переносом legacy-конфигурации. При запуске
  без параметров можно выбрать английский или русский; по умолчанию выбран
  английский.
- Полный зашифрованный backup и restore для аварийного восстановления.

UI и Authelia рассчитаны на доступ через HAProxy. Их внутренние порты нельзя
публиковать напрямую в недоверенные сети.

## Архитектура

    Клиенты
       |
       v
    HAProxy на хосте (:80/:443 и настроенные TCP-порты)
       |-- HTTP/HTTPS-бэкенды приложений
       |-- настроенные TCP-фронтенды --> TCP-бэкенды приложений
       |-- запрос авторизации -------> Authelia (127.0.0.1:9091) --> Redis
       \-- домен админки ----------> UI (127.0.0.1:5000)
                                      |
                                      \-- ограниченные helper-сокеты

    Клиенты (UDP) --> iptables DNAT (ядро) --> UDP-бэкенды приложений

Ansible в `/opt/easy-ha-proxy/venv` управляет пакетами хоста, systemd-юнитами,
конфигурацией HAProxy, сертификатами и Docker Compose-сервисами.
Непривилегированный UI использует runtime-сокет HAProxy для оперативных команд
и ограниченные helper API в `/run/easy-ha-proxy` для изменений конфигурации
хоста.

## Требования

Управляемый сервер должен иметь:

- Debian 12 или новее либо Ubuntu 22.04 или новее;
- systemd;
- архитектуру `amd64` или `arm64`;
- root-доступ либо пользователя с рабочим `sudo`;
- открытые входящие TCP-порты 80 и 443, а также порты всех настроенных TCP-прокси
  и портов проброса UDP;
- production DNS A/AAAA-записи, направляющие домены админки и Authelia на этот
  сервер;
- исходящий доступ к настроенным репозиториям пакетов ОС, GitHub, реестру
  контейнеров, Python package index, Ansible Galaxy, Snap, Let's Encrypt и
  источнику DB-IP GeoIP во время установки и ежемесячных обновлений.

Для удалённой установки на рабочей машине нужны только `bash`, `curl`, `ssh` и
`scp`. Ansible устанавливается во внутреннее виртуальное окружение проекта на
управляемом сервере.

## Быстрый запуск

Сохраните установщик в файл, чтобы его можно было просмотреть до выполнения:

```bash
curl -fsSLo /tmp/easy-ha-proxy-install.sh \
  https://raw.githubusercontent.com/CLLlAgOB/easy-ha-proxy/main/install.sh
bash /tmp/easy-ha-proxy-install.sh
```

Первый запрос предлагает English или Русский. Enter оставляет English.
Для явного или неинтерактивного запуска задайте
`EASY_HA_PROXY_LANGUAGE=en` или `ru`.
Также можно передать `--language ru` после `local` или `remote`.

Перед последней командой просмотрите сохранённый скрипт удобным редактором или
программой просмотра.

Помощник определяет состояние системы — чистая, частично настроенная или уже
управляемая — и предлагает только подходящие действия. После установки то же
меню доступно командой:

```bash
sudo easy-ha-proxy-assistant
```

Подробное поведение установщика, миграции, backup и restore описано в
[INSTALLER_README.ru.md](INSTALLER_README.ru.md).

### Установка или управление текущим сервером

```bash
bash /tmp/easy-ha-proxy-install.sh local
```

На чистом сервере помощник предлагает production- и test mode. В установленном
стеке основное действие проверяет source, helper-демоны, registry digest
Docker-образов и обновления ОС, затем предлагает только найденные обновления.
Точечные операции убраны в дополнительное подменю.

### Управление удалённым сервером

Через обычную конфигурацию SSH:

```bash
bash /tmp/easy-ha-proxy-install.sh remote admin@server.example.com
```

С запросом SSH-пароля:

```bash
bash /tmp/easy-ha-proxy-install.sh remote \
  --host 192.0.2.10 --user admin --ask-pass
```

Пароль читает OpenSSH через скрытый prompt терминала. Параметра командной строки
с самим паролем намеренно нет.

С приватным ключом:

```bash
bash /tmp/easy-ha-proxy-install.sh remote \
  --host 192.0.2.10 --user admin \
  --port 2222 --identity ~/.ssh/server
```

Из Ansible INI inventory:

```bash
bash /tmp/easy-ha-proxy-install.sh remote \
  --inventory ./ansible/inventory.ini --limit proxy01
```

Парсер inventory поддерживает `ansible_host`, `ansible_user`, `ansible_port`,
`ansible_ssh_port` и `ansible_ssh_private_key_file`. Значение
`ansible_password` намеренно игнорируется — используйте `--ask-pass`. Добавьте
`--dry-run`, чтобы проверить параметры без SSH-подключения.

### Test mode без публичного DNS

В test mode начальный сертификат управляющей части выпускает внутренний CA;
пропускаются только публичная DNS-проверка и первоначальный выпуск Let's
Encrypt. Certbot, таймер продления, deploy hooks и уведомления устанавливаются,
поэтому позднее сайты можно переключать на Let's Encrypt:

```bash
sudo easy-ha-proxy install --test-mode
```

При первой установке его можно выбрать через скачанный помощник:

```bash
bash /tmp/easy-ha-proxy-install.sh local --test-mode
```

Или на удалённой тестовой машине:

```bash
bash /tmp/easy-ha-proxy-install.sh remote \
  --test-mode admin@192.0.2.10
```

Test mode использует `ha.easy-ha-proxy.test` и `aut.easy-ha-proxy.test`,
создаёт локальную CA и SAN-сертификат, пропускает публичную DNS-проверку и
печатает готовую строку для файла `hosts`. Скопируйте с сервера
`/tmp/easy-ha-proxy-internal-ca.crt` и импортируйте только на клиентах,
которым нужно доверять этой CA. Её закрытый ключ остаётся доступен только root
в `/etc/haproxy/certificate-authorities/internal`.

Успешную тестовую установку можно перевести в production без переустановки.
Команда создаёт backup managed-конфигурации, меняет домены управляющей части и
сайтов, сохраняет пользователей, секреты, backend и TCP-прокси, запускает check
mode и применяет изменения только после подтверждения `PROMOTE`:

```bash
sudo easy-ha-proxy promote-production --new-domain example.com \
  --certificate-source internal --image latest
```

Выберите `letsencrypt` вместо `internal`, когда публичный DNS и порты 80/443
готовы. Отсутствующие DNS-записи теперь дают предупреждение и пропускают
первоначальный ACME-выпуск, но не прерывают установку.

## Первоначальная настройка

Мастер запросит:

- начальный источник сертификатов: публичный **Let's Encrypt** или встроенный
  **внутренний CA**;
- основной домен, домены админки и Authelia;
- email Let's Encrypt или администратора и часовой пояс;
- необязательные разрешённые IP/CIDR и страны GeoIP;
- логин, имя, email и пароль первого `superadmin`;
- необязательные почтовые уведомления через управляемый внутренний relay; одна
  конфигурация внешнего SMTP используется Authelia и уведомлениями о продлении
  сертификатов.

Пароль администратора хранится как Argon2id-хеш. Случайные секреты Authelia,
внутренний proxy secret и необязательный SMTP-пароль записываются в
`/etc/easy-ha-proxy/secrets.yml` с режимом `0600`.

Authelia и Certbot не подключаются к внешнему SMTP напрямую. Они передают письма
внутреннему Postfix relay, постоянная очередь которого переживает временную
недоступность внешней сети. Хост, учётные данные, режим TLS, отправителя и
получателя уведомлений затем можно менять на странице настроек Authelia в
панели управления. Тест помещает письмо в локальную очередь; внешний маршрут
подтверждается получением письма в указанном почтовом ящике.

Если доставка почты выключена, Authelia хранит только последнее локальное
уведомление. После проверки личности запросившего по доверенному каналу
`superadmin` может раскрыть и скопировать это уведомление на странице
**Настройки Authelia**. Следующий запрос перезапишет его, а перезапуск Authelia
может очистить; содержащуюся в нём короткоживущую ссылку сброса или одноразовый код
должен использовать сам пользователь — администратор не подтверждает действие
от его имени.

Установщик загружает DB-IP Country Lite в
`/etc/haproxy/geoip/releases/` и атомарно активирует согласованную пару MMDB и
country ACL для HAProxy. Systemd-таймер проверяет обновление ежедневно, но
скачивает базу не чаще одного раза для опубликованного месяца. При ошибке
загрузки, проверки, reload HAProxy или проверки панели/Authelia остаётся
активной последняя рабочая версия. GeoIP — приблизительный сигнал доступа, а
не замена аутентификации.

Тот же выбор можно передать явно: `--certificate-source letsencrypt` или
`--certificate-source internal`. Этот параметр выбирает только первоначальный
сертификат панели управления и Authelia. Внутренний вариант пропускает
публичную DNS-проверку и первоначальный публичный выпуск, но Certbot
устанавливается и остаётся доступным.
Если используется корпоративный/внешний CA, завершите первую установку с любым
начальным источником, затем на `/haproxy/certs` импортируйте публичный пакет
корневых/промежуточных CA и загрузите/выберите соответствующий серверный
сертификат в редакторе сайта.

## Повседневное управление

Локальное обновление GeoIP можно запустить и проверить отдельно:

```bash
sudo systemctl start easy-ha-proxy-geoip-update.service
sudo journalctl -u easy-ha-proxy-geoip-update.service -n 50 --no-pager
sudo cat /etc/haproxy/geoip/current/state.json
```

База используется интерфейсом даже при выключенной GeoIP-фильтрации HAProxy.
IP посетителей не отправляются внешним сервисам геолокации или флагов.

Пробросы UDP редактируются на странице **Проброс UDP**. Сохранение синхронное:
успех показывается только после того, как systemd loader установил полный набор
iptables rules; при ошибке автоматически восстанавливаются предыдущие
`udp.yml` и активные правила. Можно указать один порт или парные включительные
диапазоны одинакового размера, например `19999-20010` в `9999-10010` (до 1024
портов в одном правиле). IPv4 backend должен находиться на другом LAN-адресе
или в контейнере; loopback-адреса (`127.0.0.0/8`) намеренно запрещены. Backend
видит адрес прокси из-за MASQUERADE. Для smoke-теста на одном хосте поместите
echo-backend во временный контейнер или network namespace: так проверяется
реальный routed-путь. GeoIP-фильтрация пока не применяется к UDP.

У полной установки и обновления есть два независимых канала:

- `--source github|local` выбирает GitHub `main` или исходники, уже
  синхронизированные в `/opt/easy-ha-proxy/source`;
- `--image latest|alpha` выбирает релизный или тестовый образ HAProxy Admin.

Интерактивное меню обновления спрашивает оба значения. Чтобы одной командой
атомарно передать незакоммиченные изменения с управляющего компьютера и
применить их вместе с тестовым образом:

```bash
./install.sh remote --inventory ./inventory.ini --limit my_server \
  --sync-source . --apply --image alpha
```

Для обычного релизного обновления используйте `--action update --source github
--image latest`. Выбор сохраняется и может быть изменён при следующем
обновлении. Образ `alpha` должен уже существовать в настроенном container
registry: синхронизация исходников не собирает и не публикует Docker-образ.

```bash
sudo easy-ha-proxy status
sudo easy-ha-proxy-assistant inspect
sudo easy-ha-proxy-assistant check-updates
sudo easy-ha-proxy-assistant smart-update
sudo easy-ha-proxy plan
sudo easy-ha-proxy update
sudo easy-ha-proxy update --component daemons
sudo easy-ha-proxy update --component services
sudo easy-ha-proxy update --ui-only
sudo easy-ha-proxy configure
sudo easy-ha-proxy configure --apply
sudo easy-ha-proxy language --language ru --apply
```

Эквивалентная удалённая команда из каталога проекта на управляющем компьютере:

```bash
./install.sh remote --language ru --inventory ./inventory.ini \
  --limit my_server --action language
```

Для этого действия управляющий скрипт сначала атомарно заменяет управляемые
исходники текущей локальной версией и сохраняет прежнее дерево на сервере как
резервную копию. Поэтому команда работает и при переходе со старого CLI, в
котором команды `language` ещё не было.

- `status` проверяет текущую работоспособность сервисов, но не ищет новые
  Docker-образы в registry.
- `check-updates` сравнивает source, демоны, Docker digest и обновления ОС,
  но ничего не применяет.
- `smart-update` выполняет ту же проверку и предлагает установить найденное.
- `plan` запускает Ansible check mode без применения изменений.
- `update` обновляет управляемый checkout, зависимости и весь стек.
- `update --component daemons` обновляет только управляемые helper-демоны.
- `update --component services` применяет host-side сервисы без обновления
  контейнерных образов.
- `update --ui-only` обновляет UI и совместимый HAProxy/helper security-контур,
  сохраняя сайты, пользователей и сертификаты.
- Обычное обновление заменяет исходники и runtime-артефакты, но не управляемые
  root-YAML; для отсутствующих новых параметров используются defaults из кода.
- `configure` повторно запускает мастер, создаёт backup и сохраняет значения за
  пределами полей, которые мастер явно изменил.
- `language --language en|ru --apply` сохраняет и применяет язык помощника,
  UI по умолчанию и писем Authelia.

### Обновление из web-интерфейса

Пользователь с ролью `superadmin` может открыть в HAProxy Admin страницу
**Обновления**. Явная read-only проверка отдельно показывает обновления
управляемых исходников/полного стека, host-сервисов, вспомогательных демонов,
контейнеров Authelia, самого контейнера HAProxy Admin и пакетов ОС из текущего
APT-кэша. При простом открытии страницы ничего не проверяется и не применяется.

Выбранные компоненты применяются долговечным заданием службы
`easy-ha-proxy-updated.service`. Root-брокер принимает только фиксированные ID:
браузер не может передать команду, Ansible-теги, путь, URL репозитория или имя
образа. Состояние переживает пересоздание web-контейнера, после самообновления
страница подключается повторно. Обновление исходников и host-сервисов
блокируется при неприменённых изменениях HAProxy, а update использует общий
maintenance-lock с полным backup/restore. Пакеты ОС устанавливаются последними;
необходимость reboot показывается, но автоматическая перезагрузка из браузера
не выполняется.

Для старой установки сначала нужен один обычный полный update через controller
или CLI — он установит брокер и сокет. Локальные незакоммиченные исходники web-
страница скопировать с рабочего компьютера не может; используйте
`./install.sh remote --sync-source . --apply ...`.

Источник сертификата выбирается отдельно для каждого сайта в web UI:

- **Let's Encrypt** автоматически выпускает и продлевает публичный сертификат.
- **Внешний центр сертификации**: сначала импортируйте публичный пакет корневых
  и промежуточных CA на `/haproxy/certs`, затем в редакторе сайта загрузите
  соответствующие серверный сертификат, закрытый ключ и цепочку.
- **Внутренний центр сертификации** один раз создаёт корневой CA и по кнопке
  выпускает внутренний сертификат. Скачайте публичный корневой сертификат на
  `/haproxy/certs` и установите только в управляемые вами доверенные хранилища.

#### DNS-01 и wildcard-сертификаты

По умолчанию сайт Let's Encrypt проходит проверку ACME по HTTP-01 на порту 80.
Если этот порт недоступен — и всегда для wildcard — сайт может проверяться
через **DNS-01**.

Учётные данные провайдера сохраняются один раз на странице
`/haproxy/certs/dns-providers` как именованный профиль. Поддерживаются
Cloudflare, DigitalOcean, Route 53 и RFC 2136. Секреты пишутся в
`/etc/easy-ha-proxy/dns-providers` с доступом только для root и никогда не
возвращаются в браузер: чтобы изменить, введите заново. Каждому провайдеру
нужен свой плагин Certbot — здесь это snap, потому что и сам Certbot snap;
перечислите нужные в `dns_plugins_enabled`, и установщик их добавит.

Дальше в редакторе сайта переключите **Проверку домена** на DNS-01 и выберите
профиль. Списков имён два:

- **Alt names (SAN)** — имена маршрутизации. HAProxy сравнивает их буквально,
  поэтому wildcard здесь запрещён: конфигурация прошла бы проверку и не
  маршрутизировала бы ничего.
- **Дополнительные имена в сертификате** только расширяют сертификат. Место
  `*.example.com` — именно здесь, и после этого новые поддомены можно
  добавлять в alt names, ничего не перевыпуская.

Выбранный плагин и путь к учётным данным Certbot записывает в свою
конфигурацию продления, поэтому продление идёт без участия человека.
Установочный сценарий Ansible умеет только HTTP-01 и оставляет сайты с DNS-01
шлюзу: он выпустит их по кнопке или при применении конфигурации.

Для установок, созданных до 2026-07-04, сначала один раз выполните полное
`sudo easy-ha-proxy update` и только потом используйте точечные обновления.

### Оповещения

Каждая часть шлюза сообщает, что видит, а решает, о чём стоит сказать, один
демон. До него о заполненном диске метрик или о том, что движок защиты начал
банить, можно было узнать только из журнала.

Условие бывает **состоянием** или **событием**, и именно это различие всё
держит. Упавший backend — состояние: остаётся истинным, может восстановиться,
и подождать пять минут перед криком правильно. Неудачный бэкап — событие: оно
уже произошло, наблюдать «всё ещё падает» нечего и восстановления не будет.

Смысл движка в том, что он придерживает:

- ничего не отправляется, пока состояние не продержится свою задержку;
- горящее условие молчит до окна повтора;
- всплеск ограничивается, чтобы одна плохая минута не стала сорока письмами —
  придержанные остаются в истории;
- о восстановлении сообщается всегда, и ограничение всплеска его не глушит;
- если стало хуже, сообщение уходит сразу, не дожидаясь повтора.

Состояние, о котором перестали сообщать, считается разрешённым: умерший
производитель не должен вечно держать алерт открытым.

Задержка и получатель могут принадлежать объекту, а не правилу: сайт сохраняет
свои `alert_after` и `alert_email` из собственных настроек.

Почта использует те же настройки уведомлений, что и сообщения о сертификатах.
Необязательный **webhook** отправляет по одному JSON-объекту на уведомление и
только по HTTPS. Имя резолвится один раз, и соединение идёт именно на этот
адрес: имя, которое при повторном запросе ответит иначе, не сможет развернуть
запрос внутрь сети. Loopback и link-local запрещены, приватные адреса — только
явным переключателем, редиректы не выполняются. URL и секретный заголовок
хранятся с доступом только для root и обратно в браузер не возвращаются.

Отчётность по контракту «как получится»: остановленный демон оповещений стоит
уведомления — никогда не метрики, не бана и не перезагрузки конфигурации.

### Копии бэкапа на другой машине

Зашифрованный архив, лежащий на том сервере, который он должен восстановить, —
не резервная копия этого сервера. **Назначение** на странице `/system/backups`
отправляет готовый архив в другое место по SFTP. Уходит только уже
зашифрованный файл: парольная фраза остаётся на шлюзе, по пути ничего не
распаковывается.

Безопасным, а не просто удобным, это делают два правила.

Дальняя сторона должна быть той, которую вы закрепили. Host key хранится
вместе с назначением, строгая проверка не отключается: копировать тому, кто
ответил по этому адресу, — значит отдать архив на неспешный разбор. Сохранить
назначение без host key нельзя, это отказ, а не подстановка умолчания.
Возьмите его через `ssh-keyscan` и сверьте с тем, что сообщает та сторона.

Старое не удаляется, пока новая копия не доказана. Проверка просит дальнюю
сторону посчитать хэш, а если шелла там нет — скачивает файл обратно и
хэширует здесь. Если не сработало ни то ни другое, копия считается
непроверенной и ротация не запускается вообще. Архив заливается под именем
`.part` и переименовывается: прерванная передача не должна выглядеть как
готовый бэкап для того, кто следующим будет чистить.

**Расписание** умеет делать и отправлять копию без участия человека. Для этого
нужна парольная фраза, хранящаяся на этом хосте, — это реальное ослабление, и
оно так и трактуется: расписание отказывается включаться без неё, вместо того
чтобы молча сделать более слабый архив. Она защищает архив там, куда он
отправлен, а не от того, кто уже владеет шлюзом. Таймер только просит демон
выполнить работу, поэтому плановый бэкап берёт тот же maintenance-lock, что и
всё остальное, и не может столкнуться с восстановлением или обновлением.

**S3-совместимое хранилище** работает так же, но с более сильным
доказательством. Загрузка несёт SHA-256 архива внутри подписи SigV4, поэтому
хранилище отвергает тело, которое ей не соответствует: успешная загрузка уже
сама по себе свидетельствует, что на той стороне лежат ровно эти байты, и
второй передачи не нужно. Подпись реализована на стандартной библиотеке, а не
добавлением SDK на шлюз, и проверена против настоящей реализации S3, а не
только сама против себя. Endpoint по обычному `http://` требует явного
разрешения.

### Идентификатор запроса

У каждого запроса появляется один идентификатор, выданный на границе. Он
пишется в access-лог, уходит наверх в заголовке `X-Request-ID` и возвращается
клиенту в нём же — так пользователь может назвать одну строку с упавшей
страницы, и её можно найти и в логе, и в приложении за шлюзом.

Присланный клиентом `X-Request-ID` отбрасывается, а не возвращается обратно.
Доверять ему — значит позволить кому угодно сделать два несвязанных запроса с
одним идентификатором, а поиск по логу, который выдаёт два разных запроса,
хуже поиска, который не выдаёт ничего.

Идентификатор стоит в формате лога перед строкой запроса намеренно: парсер
движка адаптивной защиты якорится на строку запроса в конце, и так он ему не
мешает. Выключается через `request_id_enabled: false`.

### Журнал запросов

По умолчанию выключен. Если включить, движок безопасности пишет каждый
запрос, который и так читает, в отдельное ограниченное хранилище, а страница
**Журнал запросов** ищет по времени, статусу, адресу клиента, host, backend,
началу пути и идентификатору запроса — так одного названного `X-Request-ID`
достаточно, чтобы найти, что произошло.

Это окно, а не система сбора логов, и цифры объясняют почему: сутки трафика на
небольшом боевом шлюзе — около 310 000 записей. Поэтому размер держит именно
лимит объёма, а не срок хранения: самые старые записи удаляются, чтобы в него
уложиться. Ниже действует то же правило, что и у сборщика метрик, — запись
останавливается раньше, чем будет тронут резерв свободного места, потому что
диагностика не должна становиться причиной нехватки диска.

Что не сохраняется никогда: query-строка, которую движок отбрасывает до
записи, и любые заголовки, cookie и тела — их в access-логе просто нет.
Списка чувствительных параметров вести не нужно, потому что параметров нет.

Хранилище живёт на том же чтении того же файла, которое движок безопасности
уже выполняет, поэтому лишнего ввода-вывода не создаёт, — но не зависит от
него: запросы с адресов из списка исключений не участвуют в подсчёте
репутации и всё равно записываются сюда, а сам журнал работает и при
выключенной адаптивной защите.

### Экспорт в Prometheus

По умолчанию выключен: Prometheus здесь не обязательная служба. Если включить,
`/metrics` на домене админки отдаёт то, что шлюз и так знает: состояние
бэкендов, состояние хранилища мониторинга, счётчики адаптивной защиты,
состояние оповещений, остаток дней у сертификатов и результат последних
бэкапа и обновления.

Сборщик не может пройти вход через Authelia, поэтому это единственный путь
помимо локальной проверки готовности, который HAProxy пропускает мимо неё, — и
пропускает только для адресов из `metrics_scrape_sources`, где по умолчанию
только этот хост. Это первый замок. Второй — bearer-токен у самого эндпоинта;
включение экспорта без токена останавливает установку, а не публикует метрики
молча. Запрос помечается ограниченной личностью, которую приложение принимает
ровно для этого одного GET и отвергает везде ещё.

Ни одного адреса посетителя в выводе нет: репутация отдаётся счётчиками по
состояниям, а не по адресам, и каждый ярлык ограничен по длине и очищен от
всего, чем можно оборвать строку.

Неотвечающий демон стоит только своих метрик; какой именно — видно по
`easy_ha_proxy_source_up`.

### История конфигурации

Применение изменений и так защищено: шлюз снимает копию работающей
конфигурации, применяет кандидата, проверяет, что управляющая часть отвечает, и
при любом сбое автоматически возвращает предыдущую — с окном подтверждения,
после которого неподтверждённое изменение откатывается само. **История
конфигурации** добавляет то, чего не хватало: каждая подтверждённая версия
сохраняется, поэтому изменение недельной давности можно посмотреть.

Сравнивается управляемая модель — сайты, TCP-прокси и переменные, — а не
сгенерированный файл HAProxy. Переехавший backend читается как

```text
сайт  shop      изменён
        backend_ip: 192.168.1.10 → 192.168.1.11
сайт  docs      добавлен
```

а не как diff на тысячу строк сгенерированного вывода. Перестановка записей в
файле изменением не считается: порядок ничего не значит и только скрыл бы
настоящую правку.

Версии лежат в `/var/lib/easy-ha-proxy/config-history/`, каждая содержит три
управляемых YAML-файла и метаданные: когда, какая транзакция, родительская
версия и хеш содержимого. Хранятся последние 50. Запись истории — best-effort:
уже успешное изменение никогда не отменяется из-за того, что историю не удалось
сохранить, а подтверждение, ничего не поменявшее, дубликата не создаёт.

Восстановление версии возвращает её файлы на место и применяет обычным
защищённым путём — в самом восстановлении нет ничего особенного. Конфигурация
рендерится и проверяется, срабатывает та же защита от потери собственного
административного доступа (по вашему текущему адресу), а изменение всё равно
нужно подтвердить до истечения срока, иначе оно откатится само. Если что-то из
этого отказало, управляемые файлы возвращаются ровно к прежнему содержимому:
отклонённое восстановление не оставляет следов.

Саму историю ничто не пишет и не удаляет: версия появляется потому, что
изменение было подтверждено.

### Журнал изменений

Страница **Change log** отвечает, кто что поменял, когда, из чего во что и
получилось ли. Запись создаётся в момент действия и после этого не правится;
изменить или удалить её через интерфейс нельзя, потому что в API таких методов
нет.

Отклонённые попытки тоже записываются. Оператор без роли superadmin, попытавшийся
вывести сервер из-под нагрузки, оставит запись `denied` со своим именем — ровно
для этого журнал и нужен.

Секреты в файл не попадают. Значения по ключам, похожим на чувствительные —
пароль, токен, секрет, passphrase, приватный ключ, сессия, учётные данные, —
заменяются до записи, причём рекурсивно, поэтому секрет внутри описания сайта
тоже поймается. Смена пароля фиксируется как `password: changed`: сравнение
делается по настоящим значениям, чтобы изменение вообще заметить, но остаётся
только факт.

Журнал лежит в `/var/lib/easy-ha-proxy/audit/audit.db`, входит в зашифрованный
архив аварийного восстановления и хранится год с жёстким лимитом по числу
записей, чтобы не расти бесконечно. Если записать в журнал не удалось, операция
всё равно выполняется — аудит не должен стать способом сломать шлюз, — а счётчик
ошибок виден, а не замалчивается.

В журнал попадает каждое административное изменение, сделанное через интерфейс:
сайты, TCP-прокси и UDP-проброс; сертификаты, центры сертификации и профили
DNS-провайдеров; пользователи Authelia, её настройки и разбаны; ручные баны и
записи в списки исключений; выбор стран GeoIP и расписание обновления;
применение, подтверждение, откат, возврат и восстановление конфигурации;
редакторы vars; запуск, остановка и перезапуск служб; задания бэкапа,
восстановления и обновления ПО; runtime-операции с бэкендами.

Что остаётся за пределами журнала: действия, которые привилегированные демоны
выполняют по своим таймерам, а не по запросу оператора, — автоматическое
продление сертификатов, таймер обновления GeoIP и баны, которые накладывает
движок адаптивной защиты. У них свои логи, а у банов — своя страница.

### DNS-01 и wildcard-сертификаты

Wildcard-сертификат нельзя подтвердить по HTTP, а HTTP-01 бесполезен, когда
порт 80 недоступен. Страница **DNS providers** хранит учётные данные, которыми
Certbot отвечает на DNS-01 challenge.

На этой платформе Certbot ставится из snap, поэтому и его DNS-плагины — snap'ы,
причём той же версии. Перечислите нужных провайдеров в `dns_plugins_enabled`
(`cloudflare`, `digitalocean`, `route53`, `rfc2136`) и примените конфигурацию;
для провайдера, которого вы не просили, ничего не ставится. Страница показывает
по каждому провайдеру, видит ли Certbot его плагин, — отсутствие видно заранее,
а не после неудачного выпуска.

Учётные данные лежат в `/etc/easy-ha-proxy/dns-providers/`, каталог `0700`,
файлы `0600`, пишет их root-демон сертификатов. В браузер они не возвращаются
никогда: форма всегда пустая, сохранение заменяет то, что хранится. Значение с
переводом строки отклоняется — файл читает Certbot от root, и одно значение не
должно уметь дописать туда вторую директиву.

Из браузера приходит только имя профиля. Имя плагина, путь к файлу и все
аргументы Certbot берутся из фиксированной таблицы, а профиль с неустановленным
плагином отклоняется с указанием, какой snap поставить. Wildcard-имя без
DNS-профиля отклоняется до запуска Certbot, чтобы не тратить попытку из лимита
на выяснение, что HTTP-01 не подойдёт.

### Импорт сертификата или центра сертификации

На странице **Сертификаты** для этого одно поле. Дайте ему PEM, DER или
контейнер PKCS#12 (`.p12`/`.pfx`, с паролем) — файл будет прочитан, чтобы
понять, что внутри, а не по расширению.

Раскладывается по Basic Constraints самого сертификата: `CA:TRUE` — это центр,
он идёт в хранилище доверия; всё остальное — конечный сертификат, и если вместе
с ним пришёл закрытый ключ, он становится серверным. Поэтому связку из корня,
промежуточного, серверного сертификата и ключа — ровно то, что обычно выдаёт
центр сертификации — можно загрузить как есть.

Разбор файла ничего не устанавливает. Он показывает, что найдено и что будет
сделано, и только потом кнопка «Установить» действует. Поэтому же файл
отправляется дважды: шлюз не хранит между двумя запросами загрузку, которую
ещё не разобрал.

HAProxy получает конечный сертификат и промежуточные, по порядку. Корень
намеренно не включается: клиент, который ему ещё не доверяет, не начнёт
доверять оттого, что сервер приложил копию.

Если тот же файл принёс и центр сертификации, цепочка проверяется против него
до установки. Центр, уже сохранённый под этим именем, молча не перезаписывается:
одинаковое содержимое принимается, другое — требует подтверждения.

### Клиентские сертификаты (mTLS)

Сайт может требовать клиентский сертификат раньше всего остального. Это
отдельный слой, а не замена Authelia: сертификат проверяется первым, и сайт
может использовать любой из них, оба сразу или ни одного.

Импорт центра сертификации сам по себе не делает его клиентским. Отметьте его
в разделе **Аутентификация по клиентскому сертификату** на странице
сертификатов, а затем выберите у сайта. Тот, кто подписывает серверные
сертификаты самого шлюза, не должен решать, кому к нему подключаться, поэтому
это решение всегда явное.

Три режима на сайт:

- **Выключено** — ничего не меняется.
- **Необязательно** — сертификат, не прошедший проверку, отклоняется, а
  посетитель вовсе без сертификата обслуживается. Удобно, пока сертификаты ещё
  раздаются.
- **Обязательно** — без сертификата доступа нет.

Один bind обслуживает все сайты на порту 443, поэтому набор доверенных CA у них
общий. Именно поэтому сайт называет свой центр: сертификат, действительный для
шлюза, всё равно отклоняется сайтом, который доверяет другому центру. Сайты
различают их по Common Name издателя, и два центра с одинаковым именем
отклоняются в момент попытки доверять обоим — там, где это ещё можно объяснить.

Как только mTLS включён хоть где-то, рукопожатие запрашивает сертификат у
каждого соединения. Объявляются только имена доверенных центров, поэтому
браузер, у которого нет ничего от них, ничего и не спросит. Неизвестный шлюзу
сертификат тоже не рвёт соединение: ошибка TLS фиксируется, но не фатальна, а
решение принимается на уровне сайта — иначе настройка одного сайта отрезала бы
посетителей всех остальных на том же адресе.

**Отклоняемые клиентские сертификаты** принимает отпечатки SHA-256, по одному в
строке, и отклоняет именно их, не отказываясь от подписавшего их центра.
Сохранение перезагружает HAProxy — именно тогда список вступает в силу.

Проверенный сертификат доходит до бэкенда как `X-Client-Cert-Subject`,
`-Issuer`, `-Serial` и `-Fingerprint`. Как и заголовки Authelia, они сначала
срезаются с любого входящего запроса, чтобы клиент не мог назначить себе
личность, просто прислав такой заголовок.

Домен администрирования здесь намеренно не предлагается. Страховкой при
изменении конфигурации служит проверка с самой машины, а её пришлось бы
освободить от клиентского сертификата — и единственная проверка, охраняющая
применение конфигурации, не увидела бы именно ту настройку, которая с наибольшей
вероятностью закроет вам вход.

### Обслуживание бэкендов

Страница **Backends** показывает все проксируемые бэкенды и их серверы с текущим
состоянием, весом и числом сессий и даёт три операции на сервер: Ready, Drain и
Maintenance, плюс runtime-вес. Drain перестаёт направлять новый трафик, давая
текущему доработать; Maintenance убирает сервер сразу.

Состояние сохраняется. HAProxy держит административное состояние серверов
только в работающем процессе, поэтому раньше сервер, выведенный на
обслуживание, тихо возвращался под нагрузку при следующем сохранении любого
постороннего сайта — сохранение выполняет reload. Теперь шлюз пишет
`show servers state` в `/var/lib/haproxy/server-state` перед каждым reload,
который делает сам, и по таймеру для тех, которые делает не он, а HAProxy
читает файл при старте.

Следствие стоит знать: обслуживание стало липким **и после перезагрузки
машины**. Сервер, оставленный на обслуживании, там и останется, пока его не
вернут. `haproxy_server_state_enabled: false` возвращает прежнее поведение,
когда любой reload сбрасывает серверы к сгенерированной конфигурации.

Браузер никогда не отправляет текст команды Runtime API. Он называет операцию и
сервер, а приложение сверяет и то и другое с тем, что HAProxy отдаёт прямо
сейчас, и только потом собирает команду. Бэкенды, от которых зависит
доступность шлюза — админ-интерфейс, Authelia, обработчики ACME и страницы
обслуживания, стик-таблицы — не предлагаются вовсе: выведя из-под нагрузки тот,
что обслуживает открытую страницу, вы отрежете себе доступ. Смена состояния
требует роли superadmin.

### Адаптивная защита (только наблюдение)

`easy-ha-proxy-guardd.service` ищет поведение, которое существующие лимиты
увидеть не могут. Stick-таблицы HAProxy меряют интенсивность за короткие окна,
поэтому сканер, который запрашивает `/.env`, затем `/.git/config`, затем
`/phpmyadmin` с интервалом в несколько минут, не задевает ни один порог — а в
access-логе это выглядит как подпись. Поэтому движок читает оба источника: лог
даёт «что именно делал этот адрес», runtime-таблицы — «насколько сильно давит и
не забанен ли уже».

По умолчанию он наблюдает. На странице **Adaptive protection** три режима —
выключено, наблюдение и боевой; выбранный там режим перекрывает значение из
конфигурации и переживает перезапуск. Начните с наблюдения, посмотрите разбор
ниже и только потом решайте.

Боевой режим банит через ту же `tbl_ban`, которой пользуются правила HAProxy, но
со своим кодом причины: адаптивный бан виден в существующем списке банов и
снимается существующей кнопкой. У записи stick-table срок таблицы, а не ключа,
поэтому расписание ведёт сам движок: повторные находки поднимают срок по
лестнице 5 минут → 30 минут → 6 часов → 24 часа, и каждый бан снимается, когда
время вышло. Истёкший бан не выдаётся заново на тех же уликах — нужны свежие
находки, — поэтому лестница считает повторные нарушения, а не то, сколько один
скан висит в окне.

Четыре вещи не банятся никогда, каким бы ни был счёт, и ни одна из них не
настраивается:

- адрес, который **когда-либо** проходил аутентификацию, а не просто держит
  живой сеанс: runtime-авторизация истекает, и просроченный сеанс не должен
  превращать живого пользователя в кандидата на бан;
- всё, что уже исключают ACL HAProxy;
- IPv6 — путь бана до него не достаёт;
- бан, который поставил сам HAProxy: перед снятием движок перечитывает код
  причины, поэтому снятие адаптивного бана никогда не сотрёт бан по rate-limit.

Возврат в наблюдение снимает все баны, поставленные движком. Баны, оставшиеся
после аварийного завершения, подбираются на следующем цикле: запись в
stick-table переживает процесс, который её поставил.

Движок никогда не трогает то, что HAProxy и так исключает. Он повторяет те же
четыре ACL — глобальный whitelist, список админских IP, GeoIP-whitelist и
адреса, прошедшие аутентификацию через Authelia, — и считает 451 запросом,
который уже отклонил GeoIP. Баны, когда дойдёт до них, будут только IPv4:
`tbl_ban` — IPv4-таблица, а firewall-набор `inet`, поэтому адреса, по которым
действовать нельзя, так и помечаются вместо накопления бесполезного счёта.

Хранятся только значимые события, а не сам трафик. Query string убирается прямо
при разборе строки — в access-логе реально встречается `?token=...`, — а пути
нормализуются и обрезаются по длине до попадания в базу. Рабочий набор по
адресам ограничен с двух сторон: и число адресов, и история путей у каждого.

Находки делятся на три уровня уверенности. Запрос пути, который нужен только
сканеру — `/.env`, `/.git/config`, `/phpmyadmin`, — это высокая уверенность и
своя категория. Слабее: перебор разных несуществующих путей, повторные запросы
без валидного Host и короткооконные счётчики частоты и ошибок, которые HAProxy
и так ведёт. Отсутствующая картинка или стиль сигналом не считаются вовсе.
Счёт складывается с ограничением вклада каждой категории: пятьдесят разных
WordPress-URL — это одна находка «искал WordPress», а не пятьдесят, и
интересен как раз адрес, попробовавший несколько несвязанных технологий.
`LOW_AND_SLOW_SCANNER` намеренно не смотрит на частоту: чем медленнее скан, тем
меньше его видят существующие лимиты.

Вклад событий затухает со временем, а не вычитается по таймеру, поэтому счёт —
чистая функция от сохранённых событий. Изменение любого веса пересчитывает всю
историю сразу, без ещё одной недели наблюдения.

Замер на сутках реального трафика двухъядерного шлюза — 310 697 строк лога,
308 251 запрос:

| | |
| --- | --- |
| Разбор и детекция | 37,7 мкс на запрос, 11,7 с CPU за сутки |
| При 100 запросах в секунду | 0,38 % одного ядра |
| Пиковая память | 26 МиБ |
| Запросов свёрнуто в события | 308 251 → 285 (0,9 на 1000 запросов) |
| Адресов, дошедших до HIGH_RISK | 1 из 90 оценённых |

У этого единственного адреса — две несвязанные технологические категории,
запросы без валидного Host и срабатывание правила медленного скана: ровно тот
профиль, ради которого движок и делается.

#### Разбор без блокировки

Пункт **Adaptive protection** в навигации HAProxy — это место, где такой разбор
и делается. Он показывает, сколько адресов оценено, скольких блокировка забанила
бы, и — это и есть цифра, по которой решается, можно ли включать enforce, —
сколько из них потом успешно прошли аутентификацию. Адрес, по которому движок
хотел действовать, а он оказался живым пользователем, — это тот сбой, который
надо поймать до того, как что-то начнёт блокироваться.

По клику на адрес видно каждую находку, давшую вклад в счёт: когда произошла,
сколько дала баллов и почему — `учтено`, `достигнут предел категории` или `уже
отклонено шлюзом`. Забанить со страницы нельзя: такого элемента управления нет,
потому что движок этого не умеет.

Симулятор весов пересчитывает сохранённую историю с другими весами, пределами и
затуханием, ничего не сохраняя: предложенное изменение можно оценить на уже
случившемся трафике, а не ждать ещё неделю. Снижение предела категории ниже веса
самого события ничего не меняет — по замыслу предел ограничивает повторы находки,
а не ценность одной.

### Страница мониторинга

Пункт **Monitoring** в навигации HAProxy показывает то, что записал сборщик.
Карточки обзора дают запросы в секунду, текущие соединения, трафик, разбивку
2xx/3xx/4xx/5xx и состояние backend за выбранный период; ниже — графики
запросов, трафика, классов ответов, времени ответа и соединений. Периоды — от
1 часа до 1 года, фильтр сайта переключает между суммой по всем frontend и
одним конкретным backend.

Разрешение выбирает сервер: минутные строки для периода до суток, часовые для
более длинных, с дополнительной группировкой на самых больших, чтобы график
никогда не возвращал больше 1500 точек. Текущее разрешение показано на странице.

Блок доступности рисует по одной полосе на каждый backend и сервер: зелёное —
доступен, красное — недоступен, жёлтое — maintenance и drain; рядом текущее
состояние, процент доступности и суммарное время недоступности за период.
Смены состояния хранятся как переходы, а не как ежеминутный слепок, поэтому
сервер, который месяц в UP, стоит одну строку, а полоса всё равно покрывает
весь период.

Карточка хранилища показывает размер базы, WAL и суммарный объём против лимита,
свободное место против резерва, измеренный прирост за неделю и действующий
ретеншн. Прирост подписан как измерение прошлого, а не прогноз.

Страница только читает и деградирует честно: если сборщик не отвечает — так и
написано, вместо нулей на графиках; если запись истории на паузе — показана
причина. Ни то, ни другое не влияет на HAProxy.

### Сбор исторической статистики

`easy-ha-proxy-metricsd.service` сохраняет, как шлюз вёл себя во времени. Демон
опрашивает runtime-сокет HAProxy каждые 10 секунд, копит выборки в памяти и
записывает одну строку на объект в минуту в локальную базу SQLite. Он только
читает HAProxy и пишет собственные файлы, поэтому остановленный, сломанный или
выключенный сборщик никак не влияет на трафик.

Данные хранятся в двух разрешениях. Минутные строки живут 7 дней для frontend и
backend и 24 часа для отдельных серверов; часовые агрегаты — год. На хосте с
большим числом бэкендов основной объём дают именно строки серверов, поэтому
минутное разрешение теряется у них первым. Переходы `UP`/`DOWN` у backend и
серверов сохраняются как события, а не как ежеминутный слепок статуса.

Значения по умолчанию заданы переменными `metricsd_*` роли `haproxy-admin` и
рендерятся в `/opt/haproxy-admin/metricsd.json`. При `metricsd_enabled: false`
сервис остаётся установленным и простаивает. База намеренно не входит в архив
аварийного восстановления: она может быть большой и не нужна для возврата
шлюза в рабочее состояние.

Сборщик отдаёт read-only Unix-сокет для административного интерфейса:

```text
/run/easy-ha-proxy/easy-ha-proxy-metricsd.sock
  GET /api/v1/metrics/health    состояние сборщика и базы
  GET /api/v1/metrics/storage   размеры, лимиты, состояние хранилища и тренд
```

#### Защита дискового пространства

Мониторинг ни при каких условиях не должен стать причиной переполнения диска.
Два лимита считаются от той файловой системы, на которой реально лежит база —
это может быть отдельный том, а не корневой раздел:

- лимит на всё, что принадлежит мониторингу: база, её WAL и shm-файл вместе.
  `auto` — меньшее из 5 GiB и десятой части файловой системы;
- резерв свободного места, который действует даже при снятом лимите на базу.
  `auto` — не меньше 2 GiB, не больше 10 GiB, иначе десятая часть тома.

По мере приближения к любому из лимитов сборщик показывает `WARNING`, затем
`PRESSURE` и урезает ретеншн по шагам: минутные строки до 3 дней, затем до
1 дня, часовые — до 180, 90 и наконец 30 дней. Минуты всегда сначала
сворачиваются в часы и только потом удаляются: потерять разрешение лучше, чем
потерять долгую историю.

Если урезания не хватило, состояние становится `CRITICAL` и запись истории
останавливается:

```text
Историческая статистика приостановлена.
Достигнут порог безопасного запаса свободного места.
Трафик HAProxy не затронут.
```

На паузе сборщик продолжает опрашивать HAProxy и считать в памяти, поэтому
после возобновления ничего не удваивается — затронутые минуты просто теряются,
а не копятся. Запись возобновляется только когда свободного места стало
заметно больше резерва, чтобы не «дёргаться» вокруг порога.

Освобождение места всегда идёт через incremental vacuum. Полный `VACUUM` не
выполняется никогда: ему нужно место под вторую копию базы — ровно то, чего при
нехватке диска и нет.

Сервис отображается на странице **Health** наравне с остальными helper-демонами.
Сборщик, последний удачный опрос которого слишком стар, показывается как
degraded, даже если сам процесс работает.

### Основные пути

- `/etc/easy-ha-proxy` — управляемая конфигурация и секреты;
- `/opt/easy-ha-proxy/source` — управляемый checkout исходников;
- `/opt/easy-ha-proxy/venv` — внутреннее окружение Python и Ansible;
- `/etc/haproxy` — конфигурация HAProxy и сертификаты;
- `/etc/haproxy/certificate-authorities` — импортированные пакеты CA и закрытый
  ключ внутреннего CA, доступный только root;
- `/opt/authelia` и `/opt/haproxy-admin` — конфигурация и данные контейнеров;
- `/run/easy-ha-proxy` — ограниченные helper-сокеты;
- `/run/haproxy/admin.sock` — HAProxy runtime socket;
- `/var/lib/easy-ha-proxy/metrics/metrics.db` — историческая статистика, не
  входит в зашифрованный backup.

### Зашифрованный backup и restore

Пользователь с ролью `superadmin` может открыть в web-интерфейсе страницу
**Бэкап и восстановление**, создать и скачать полный зашифрованный снимок,
проверить архив и восстановить его. Для снимка, который остался на этом же
сервере, достаточно нажать **Восстановить**; на другой сервер загружается ранее
скачанный файл `.enc`. Задания выполняются отдельной службой на
хосте, поэтому страница повторно подключится после краткой приостановки или
пересоздания контейнера приложения. Перед изменением файлов restore обязательно
проверяет архив без записи на сервер и требует точно ввести `RESTORE`. Затем
заменяются управляемая конфигурация, секреты, пользователи и данные Authelia,
сертификаты и данные приложения. При проверке измеряется распакованный объём и
до остановки служб проверяется место для восстановления и автоматического
отката. SSH-ключи по умолчанию не включаются.

Через web-интерфейс можно восстановить этот сервер или другой сервер, на который
уже установлен свежий минимальный стек easy-ha-proxy. Для полностью пустого
Debian/Ubuntu используйте controller-команду ниже. Если распаковка или применение
конфигурации завершится ошибкой, защищённый снимок до restore применяется
автоматически. Этот временный локальный safety-снимок удаляется после успешного
restore или отката и сохраняется только при ошибке самого автоматического
отката.

Из checkout на управляющей машине:

```bash
bash ./install.sh backup-full inventory.ini proxy01
bash ./install.sh restore-full \
  /path/to/easy-ha-proxy-full-YYYYMMDDTHHMMSSZ.tar.gz.enc \
  inventory-new.ini proxy-new fresh
```

SSH-ключи включаются и восстанавливаются только после отдельных подтверждений.
Даже зашифрованный backup содержит чувствительные production-данные: не
добавляйте его в Git и не размещайте в публичном хранилище артефактов. Парольная
фраза не сохраняется: скачайте архив `.enc` и файл `.sha256`, а парольную фразу
храните отдельно в менеджере паролей.

## Ручной режим Ansible

Рекомендуемый интерфейс — штатный помощник. После создания локального inventory
и исключённых из Git конфигурационных файлов, описанных в
[ansible/README.md](ansible/README.md), остаётся доступен прямой удалённый
запуск Ansible:

```bash
cd ansible
ansible-galaxy collection install -r requirements.yml
ansible-playbook --syntax-check -i inventory.ini easy-ha-proxy.yml
ansible-playbook -i inventory.ini easy-ha-proxy.yml -t status
```

Запуск playbook без тегов включает обновление пакетов ОС. Playbook никогда не
перезагружает узел с `ansible_connection=local`: после обновления помощник
easy-ha-proxy запрашивает перезагрузку, по умолчанию откладывая её. При прямом
запуске playbook перезагрузите сервер вручную или явно передайте
`easy_ha_proxy_reboot_after_upgrade=true` для целевого узла через SSH. Проверьте план и теги
перед применением в production.

Inventory, host variables и секреты намеренно не входят в публичный репозиторий.

## Разработка и проверки

Все тесты лежат в `.tests/`, которая находится вне контекста сборки Docker,
поэтому ничего из них не попадает в рабочий образ. `.github/workflows/ci.yml`
выполняет ровно приведённые ниже команды на Python 3.10, 3.11 и 3.12.

```bash
python3 -m py_compile installer/easy_ha_proxy.py
python3 -m compileall -q docker/app \
  ansible/roles/authelia/files \
  ansible/roles/haproxy-admin/files
PYTHONPATH=installer python3 -m unittest discover -s .tests -p 'test_*.py'

bash -n install.sh install-local.sh install-remote.sh \
  easy-ha-proxy-helper.sh installer/easy-ha-proxy

ansible-galaxy collection install -r ansible/requirements.yml
ansible-playbook --syntax-check -i localhost, ansible/easy-ha-proxy.yml
```

Переводы UI хранятся в JSON-каталогах в
[`docker/app/haproxy_admin/translations/`](docker/app/haproxy_admin/translations/README.md).
После добавления каталога новый язык автоматически появляется в переключателе;
копировать шаблоны не требуется. Канонический язык исходников — английский;
остальные каталоги сопоставляют английские строки с переводом.

## Безопасность

- Не публикуйте напрямую порты UI `5000` и Authelia `9091`.
- Оставляйте `/etc/easy-ha-proxy` во владении root и сохраняйте режим `0600` у
  `secrets.yml`.
- Ограничивайте административный доступ через Authelia и, где возможно,
  IP/CIDR allowlists.
- Просматривайте скачанные install-скрипты и план Ansible до применения в
  production.
- При утечке приватного ключа или credential смените его. Удаления файла из
  последнего commit недостаточно.
- Локальная тестовая CA предназначена только для изолированного тестирования.

Автоматические проверки не заменяют аудит безопасности конкретного deployment.

## Лицензия

Если в файле не указано иное, оригинальный код и документация проекта
распространяются по [GNU General Public License v3.0 or later](LICENSE)
(`GPL-3.0-or-later`).

Встроенные Lua-файлы сохраняют собственные notices, перечисленные в
[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md):

- `ansible/roles/authelia/files/lua/auth-request.lua` — MIT;
- `ansible/roles/authelia/files/lua/json.lua` — MIT;
- `ansible/roles/authelia/files/lua/http.lua` — Apache-2.0;
- адаптированные notification templates Authelia в
  `ansible/roles/authelia/templates/` — Apache-2.0.

Внешние пакеты и контейнерные образы распространяются по собственным лицензиям
и не перелицензируются этим репозиторием.
